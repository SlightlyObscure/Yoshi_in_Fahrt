#pragma once

//NOTE: This is a mock implementation that needs to be replaced once a proper worldmap is to be implemented
//this struct should hold a 2D array ready to be parsed into the world map or a direct representation of a portion of the worldmap

struct Mapdata {
	double mockData[16] = { 0 };
};