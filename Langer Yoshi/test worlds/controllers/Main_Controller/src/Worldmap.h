#pragma once

#include "Mapdata.h"

//NOTE: This is a mock implementation that needs to be replaced once a proper worldmap is to be implemented
//This class should hold recorded sensor data in a format that makes calculating circumnavigation convenient

struct Worldmap {
	Mapdata mockMap;
};